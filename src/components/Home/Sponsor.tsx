import React from "react";
import { Box } from "@mui/material";

const Sponsor = () => {
  return (
    <Box
      component="section"
      sx={{
        py: "150px",
      }}
    ></Box>
  );
};

export default Sponsor;
