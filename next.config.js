/** @type {import('next').NextConfig} */
module.exports = {
  reactStrictMode: true,
  // basePath: process.env.NODE_ENV === "production" && "/podcast",
  images: {
    domains: ["i.ibb.co"],
  },
};
